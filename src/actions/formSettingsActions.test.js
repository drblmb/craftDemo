// import {
//   setFormSettings,
// } from 'actions/formSettingsActions';
// import { SET_TRANSACTIONS_FILTER } from 'constants/actionTypes';

// TODO: Finish Unit Tests
describe('Form Settings Actions', () => {
  const dispatch = jest.fn();

  beforeEach(() => {
    dispatch.mockClear();
  });

  describe('saveFormSettings()', () => {
    it('should dispatch action to save formSettings', () => {
      expect(true).toBe(true);
    });
  });
});
